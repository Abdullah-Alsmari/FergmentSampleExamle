<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/firstFragment2">
    <fragment
        android:id="@+id/firstFragment2"
        android:name="com.example.myapplication.FirstFragment"
        android:label="first"
        tools:layout="@layout/fragment_first">
        <action
            android:id="@+id/action_firstFragment2_to_secoundFragment"
            app:destination="@id/secoundFragment" />
    </fragment>
    <fragment
        android:id="@+id/secoundFragment"
        android:name="com.example.myapplication.SecoundFragment"
        android:label="secound"
        tools:layout="@layout/fragment_secound">
        <argument
            android:name="info"
            app:argType="string" />
    </fragment>
</navigation>